# A Simple Neural Network in Hoon

Written by ~mopfel-winrux and ~lagrev-nocfep, including some code from ~litlep-nibbyt.

Usage:

```
+conjgrad  shows off conjugate gradient method
+nn        shows off solver
```
